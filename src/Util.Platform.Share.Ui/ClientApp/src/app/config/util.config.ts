import { AppConfig } from 'util-angular';
import { urlConfig } from './url.config';

/**
* Util应用配置
*/
export const utilConfig: AppConfig = {
    /**
    * Api端点地址
    */
    apiEndpoint: urlConfig.apiEndpointUrl
}
